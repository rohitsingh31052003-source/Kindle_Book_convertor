"""Format-independent document model.

This package will hold the intermediate representation produced by the
PDF layer and consumed by the EPUB layer (e.g. ``models``,
``structure``, ``cleanup``). Nothing is implemented here yet.
"""